# Gemini-only sweep (Sprint 4 / Phase 10) — run the 3rd generator over the existing
# gold index, then merge with results/baseline.jsonl (OpenAI + Anthropic) for a 3-way
# comparison. Avoids re-running the two models already captured in baseline.jsonl.
# Judge stays OpenAI (gpt-5-nano) — independence preserved (Gemini never judges).

models:
  - model_id: "gemini-2.5-flash-lite"
    system: "google"

judge_model: "gpt-5-nano-2025-08-07"

limit: null
k: 10
output_dir: "results"
run_id: "gemini"

cost_ceiling_usd: 5.0

prices:
  gemini-2.5-flash-lite:
    input_usd_per_1m: 0.10
    output_usd_per_1m: 0.40
  # Judge price (gpt-5-nano) — needed for judge-call cost accounting.
  gpt-5-nano-2025-08-07:
    input_usd_per_1m: 0.05
    output_usd_per_1m: 0.40
